import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder so `flutter test` has something to run - without a test/ folder
  // the CI job fails with "no test files found". Replace with real tests.
  test('REBESTA Partner app test', () {
    expect(true, isTrue);
  });
}
