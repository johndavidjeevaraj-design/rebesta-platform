import 'package:flutter_test/flutter_test.dart';

void main() {
  // This replaces the stock Flutter template test. That one pumped MyApp() and
  // looked for a counter with an Icons.add button - neither exists in this app,
  // so it could only ever fail. Replace this with real widget tests as you add
  // them (see rebesta_customers/lib/presentation for screens worth covering).
  test('REBESTA Customers app test', () {
    expect(true, isTrue);
  });
}
